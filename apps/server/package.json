{
  "name": "@fleetops/server",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint . --ext .ts",
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@fleetops/sim-engine": "workspace:*",
    "fastify": "^4.28.1"
  },
  "devDependencies": {
    "@sinclair/typebox": "^0.34.38",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "supertest": "^7.0.0",
    "vitest": "^1.6.0"
  }
}
